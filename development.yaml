apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
  labels:
    app: nginx
    version: v1.1
spec:
  containers:
    - name: nginx
      image: nginx:latest
      ports:
        - containerPort: 80
