apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
  labels:
    app: nginx
    version: v1.2
spec:
  containers:
    - name: nginx
      image: python:latest
      ports:
        - containerPort: 80
